
```sql
CREATE INDEX IDX_WORKORDER_PK on edn_work_order(WORKORDER_NO(32));
CREATE INDEX IDX_WORKORDER_RES_PK on edn_work_order_res(WORKORDER_NO, WORKORDER_RES_ID);
CREATE INDEX IDX_WORKORDER_RES_ID on edn_work_order_res(WORKORDER_RES_ID);
CREATE INDEX IDX_WORKORDER_ACTION_PK on edn_workorder_action_log(WORKORDER_NO, SEQ);
CREATE INDEX IDX_WORKORDER_LAYER_PK on edn_workorder_layer_info(WORKORDER_NO, RES_SPEC, DATASET_NAME);
create index IDX_SYN_DETAIL_ID on edn_sync_detail(RES_ID);

-- 判断死锁
SELECT * FROM INFORMATION_SCHEMA.INNODB_LOCKS; 
SELECT * FROM INFORMATION_SCHEMA.INNODB_LOCK_WAITS; 
show OPEN TABLES where In_use > 0;
show processlist;
kill 318;
```

